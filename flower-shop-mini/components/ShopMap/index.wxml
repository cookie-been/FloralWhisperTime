<view class="map-card">
  <map class="map" latitude="{{shop.latitude}}" longitude="{{shop.longitude}}" markers="{{markers}}" />
  <button class="nav-button" bindtap="openLocation">导航到店</button>
</view>
